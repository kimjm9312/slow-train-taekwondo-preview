import { useState } from "react";
import { Alert, ScrollView, Text, TextInput, TouchableOpacity, View } from "react-native";
import { api, json } from "../api/client";
import { styles } from "../theme";
import type { User } from "../types";

export function ProfileScreen({user,onChanged,onLogout}:{user:User;onChanged:(user:User)=>void;onLogout:()=>void}){const[name,setName]=useState(user.name),[phone,setPhone]=useState(user.phone);async function save(){try{const data=await api<{profile:User}>("/api/v1/profile",json("PATCH",{name,phone}));onChanged(data.profile);Alert.alert("내 정보","저장했습니다.");}catch(error){Alert.alert("내 정보",error instanceof Error?error.message:"저장하지 못했습니다.");}}return <ScrollView style={styles.screen} contentContainerStyle={styles.content}><Text style={styles.eyebrow}>MY SLOW TRAIN</Text><Text style={styles.title}>내 정보</Text><View style={[styles.card,{marginTop:18}]}><Text style={{fontWeight:"800",marginBottom:8}}>아이디</Text><Text style={[styles.muted,{marginBottom:16}]}>{user.username}</Text><Text style={{fontWeight:"800",marginBottom:8}}>보호자 이름</Text><TextInput style={styles.input} value={name} onChangeText={setName}/><Text style={{fontWeight:"800",marginBottom:8}}>휴대전화</Text><TextInput style={styles.input} value={phone} onChangeText={setPhone} keyboardType="phone-pad"/><TouchableOpacity style={styles.button} onPress={save}><Text style={styles.buttonText}>개인정보 저장</Text></TouchableOpacity></View><TouchableOpacity onPress={onLogout} style={{padding:18,alignItems:"center"}}><Text style={styles.muted}>로그아웃</Text></TouchableOpacity></ScrollView>}
